# Domain Docs

Before exploring, read the root `CONTEXT.md` and relevant ADRs under `docs/adr/`. If they do not exist, proceed silently; create them lazily only when domain terminology or architectural decisions are resolved.

This is a single-context repository:

```
/
├── CONTEXT.md
└── docs/adr/
```

Use terminology defined by `CONTEXT.md`. Explicitly surface any proposal that contradicts an existing ADR.
